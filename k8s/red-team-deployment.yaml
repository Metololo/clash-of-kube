apiVersion: apps/v1
kind: Deployment
metadata:
  name: red-soldiers
spec:
  replicas: 1
  selector:
    matchLabels:
      app: soldier
      team: red
  template:
    metadata:
      labels:
        app: soldier
        team: red
    spec:
      containers:
      - name: soldier
        image: metooo/soldier:v1
        env:
        - name: SOLDIER_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: TEAM
          value: red
        - name: HEALTH
          value: "150"
        - name: ATTACK_POWER
          value: "20"
        - name: PORT
          value: "8080"
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: red-team
spec:
  selector:
    app: soldier
    team: red
  ports:
    - port: 80
      targetPort: 8080